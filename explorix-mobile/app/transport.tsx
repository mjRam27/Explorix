import { View, Text } from 'react-native';

export default function TransportScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Text>Transport Screen (wired to backend later)</Text>
    </View>
  );
}
